<Page x:Class="Fitpad.View.Pages.NewsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:Fitpad.ViewModel.PagesViewModels"
      Title="NewsPage">

    <Page.DataContext>
        <vm:NewsViewModel />
    </Page.DataContext>

    <Grid>
        <ListView ItemsSource="{Binding News}" Margin="0" SelectionChanged="ListView_SelectionChanged" HorizontalAlignment="Center">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="White" BorderThickness="1" CornerRadius="10" Margin="5" Padding="50">
                        <StackPanel Orientation="Vertical" Width="555" Margin="0,-35,0,0">
                            <Image Source="{Binding UrlToImage}" Height="370" Width="550" Stretch="UniformToFill" Margin="0,0,0,0" />
                            <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="Bold" Margin="5,20,5,0"
                                       TextWrapping="Wrap" />
                            <TextBlock Text="{Binding Description}" FontSize="14" TextWrapping="Wrap" Margin="5,20,5,10" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
