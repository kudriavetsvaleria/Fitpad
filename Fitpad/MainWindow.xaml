<Window x:Class="Fitpad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Fitpad.Converters"
        Title="Fitpad" Height="600" Width="900">
    <Window.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="#FFDDDDDD" Visibility="{Binding IsNavigationVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Button Content="Новини" Command="{Binding ShowNewsCommand}" Margin="5" Height="60"/>
            <Button Content="Збережене" Command="{Binding ShowFavoritesCommand}" Margin="5" Height="60"/>
            <Button Content="Харчування" Command="{Binding ShowNutritionCommand}" Margin="5" Height="60"/>
            <Button Content="Тренування" Command="{Binding ShowWorkoutsCommand}" Margin="5" Height="60"/>
            <Button Content="Профіль" Command="{Binding ShowProfileCommand}" Margin="5" Height="60"/>
        </StackPanel>

        <!-- Изменено с ContentControl на Frame -->
        <Frame Grid.Column="1" Content="{Binding CurrentPage}" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
